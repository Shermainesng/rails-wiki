<h1><%= @article.title %></h1>
<h3><%= @article.content %></h3>
<%= link_to 'Back to articles', articles_path %> || <%= link_to 'Edit article', edit_article_path(@article)%> || <%= link_to "Delete", article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %>
